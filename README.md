# wifish

## 1. wifi.sh

#### Работает через iwd

Установить пакет:

```sudo apt install iwd```

Скачать файл и сделать его исполняемым:

```sudo chmod +x ~/wifi.sh```

## 2. wifi2.sh

#### Работает через wpa-cli

Скачать файл и сделать его исполняемым:

```sudo chmod +x ~/wifi2.sh```
